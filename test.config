%% vim: ft=erlang :

%% test with curl:
%% $ curl localhost:9090/test/jsonerl/io/format -d '["hello, ~s~n", ["world"]]'

[{serval,
  [{port, 9090}, {static_filepath, "/tmp/a"}, {static_urlpath, "/blah"},
   {api_urlpath, "/test"}, {safe_modules, [io]}]}].

